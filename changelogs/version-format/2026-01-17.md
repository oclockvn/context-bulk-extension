## Summary
- fix assembly/file version formatting for multi-target packages

## Details
- remove the extra `.0` suffix so `AssemblyVersion` and `FileVersion` remain 4-part values
- keep `Version` and `BaseVersion` unchanged to preserve package versioning
