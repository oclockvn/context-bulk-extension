## Summary
- consolidate to single project structure, removing separate SqlServer packaging project

## Details
- remove `NugetPackages/ContextBulkExtension.SqlServer` project folder and all references
- update `ContextBulkExtension.csproj` and `ContextBulkExtension.Net8.csproj` to be packable (`IsPackable=true`)
- remove provider-based conditional `PackageId` logic from `Directory.Build.props`
- update solution files to remove SqlServer project references
- package name is now `ContextBulkExtension` for both net8 and net10 targets
